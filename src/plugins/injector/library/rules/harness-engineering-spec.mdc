---
description: 研发 harness 必读：工程设计规范与流程门禁
alwaysApply: true
---

# 工程设计规范（必读）

任何设计与编码都遵循以下原则；写 `design.md` 时需说明方案如何满足：

- **单一职责（SRP）**：一个模块/类/函数只做一件事，只有一个变化理由。
- **开放封闭（OCP）**：对扩展开放、对修改封闭；新增能力靠扩展点/插件，不改既有稳定代码。
- **依赖倒置（DIP）**：高层不依赖低层实现，二者都依赖抽象（接口）；实现可替换/注入。
- **高内聚低耦合**：相关逻辑收敛在边界内，跨模块走清晰窄接口。

落地：插件通过 `PluginContext` 抽象访问能力（DIP）；新功能优先做成插件/扩展点（OCP）；模块边界对齐 `docs/README.md §二`。

# 文档规范

- 新需求先写 `prd.md` + `design.md` 并通过人工门禁，**未过门禁禁止实现类编码**（见 `docs/HARNESS.md`）。
- 文档**简洁、不长篇大论**；设计文档**图文并茂**（关键流程/框架/时序/类关系配 mermaid 或 drawio）。
- `design.md` 按 `docs/README.md §八` 标准结构：背景 → 需求目标 → 待确认问题 → 现状分析 → 初步方案。
- 文档目录遵循 `docs/README.md`：模块自洽、docs 内聚。
