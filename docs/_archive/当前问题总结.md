
1.codebuddy上配置流程比较复杂，因为Hooks节点需要在Knot上配置
2.配置流程比较复杂，如果想要快速配置一个api，流程好像并没有那么简单，可不可以像openclaw一样，一键安装，提供一个清晰的命令行安装流程
3.启动流程比较麻烦，每次开机需要重新启动，动不动就忘记开了，需要提供什么配置让他可以无感默认运行启动，不需要总是关心这个服务挂掉了，有哪些解决方案，用户体验不太好
4.需要确认下，是不是能拿到详细的对话数据沉淀，还是只拿到一些边边角角的对话内容
5.怎么用这个问题？好不好用这个问题，我配置到cursor或者codebuddy后，如何让这些ide主动会去调用，自主调用，自主调用回忆，可以通过什么方式，是不是要匹配对应的skills，让他能够天然的使用起来，而不是用户提醒